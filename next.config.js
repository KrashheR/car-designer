// import type { NextConfig } from "next"; // Removed this line

const nextConfig = {
  // Removed type annotation
  /* config options here */
};

module.exports = nextConfig; // Changed from export default
